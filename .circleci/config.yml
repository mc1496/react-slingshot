version: 2.1

executors:
  my-executor:
    docker:
      - image: cimg/node:19.1.0

jobs:
  build:
    executor: my-executor
    steps:
      - checkout
      - run: echo "BUILD"

  test:
    executor: my-executor
    steps:
      - checkout
      - run: echo "TEST"

  analyze:
    executor: my-executor
    steps:
      - checkout
      - run: echo "ANALYZE"

workflows:
  bta:
    jobs:
      - build
      - test:
          requires:
            - build
      - analyze:
          requires:
            - test


# VS Code Extension Version: 1.3.0